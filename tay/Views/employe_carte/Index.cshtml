@model IEnumerable<tay.Models.employe_carte>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_test.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-primary m-0">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CarteModel.reference)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employe.Nom_Prenom)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarteModel.reference)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employe.Nom_Prenom)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                @Html.ActionLink("Details", "Details", new { id = item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id })
            </td>
        </tr>
    }

</table>
